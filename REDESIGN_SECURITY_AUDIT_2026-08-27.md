# تدقيق إعادة التصميم والأمان — منصة ذي قار الرقمية

**التاريخ:** 27 آب 2026

## ملخص الحالة الحالية

المنصة منشورة وتعمل على Railway، وتحتوي على OTP خادمي، تخزين مشفر للهوية والفيديو والمرفقات، مراجعة موظف، PDF وQR، أخبار، سجل دوائر، وغرفة عمليات. إلا أن التصميم ما زال يعتمد مكوّناً واحداً كبيراً في `src/App.tsx` وملف CSS كبيراً، ما يجعل الصيانة والتناسق أصعب. كما أن الشريط القانوني أعلى كل صفحة يطغى بصرياً على الهوية، ويوجد اسم `DemoRibbon` ومسارات `/demo` قديمة رغم أن الواجهة لم تعد تصف نفسها كعرض تجريبي.

## نتائج التصميم

| البند | الحالة | الإجراء المطلوب |
|---|---|---|
| الهوم سكرين | الهيكل جيد لكن البطل شاحب ويحتوي أرقاماً ثابتة وصورة واحدة؛ الانطباع أقل حداثة من المطلوب | إعادة بناء البطل بتباين أقوى، حركة خفيفة، شريط خدمات حي، وصور مؤسسية مرتبطة بذي قار |
| الموبايل | أعلى الصفحة صار مقروءاً، لكن بعض لوحات الإدارة طويلة ومكدسة | توحيد شبكة 8px، شريط تنقل سفلي عملي، بطاقات مختصرة، وسحب/طي الأقسام |
| الأرقام | معظم العرض يستخدم `en-GB`/`en-US` | إنشاء formatter مركزي ومنع أي أرقام عربية عبر فحص آلي |
| الطابع الرسمي | الشعار الرسمي موجود، لكن أسماء داخلية وتجريبية ما زالت في الكود | إزالة `DemoRibbon`، نقل النص القانوني إلى الشروط/التذييل، وتحديث أسماء API والمسارات |
| الحركات | توجد Framer Motion في أجزاء محدودة | اعتماد حركات 160–260ms، مع `prefers-reduced-motion` ودون إفراط |

## نتائج الأمان

| البند | الحالة الحالية | الخطورة/الإجراء |
|---|---|---|
| CORS | `origin: true` لكل أصل | تقييده إلى النطاق الإنتاجي والنطاقات المصرح بها |
| Security headers | لا يوجد Helmet/CSP صريح | إضافة CSP وHSTS وX-Frame-Options وReferrer-Policy وPermissions-Policy |
| المصادقة | بوابة الموظف وغرفة العمليات لا تستخدم جلسات مستخدمين حقيقية؛ مراجعة الوسائط تعتمد رمز وصول | بناء جلسات آمنة وأدوار مواطن/موظف/مراجع/مدير، وإلغاء مشاركة رمز واحد |
| Rate limiting | مطبق على OTP فقط | إضافة حدود عامة للـAPI، والرفع، والتحقق، وتسجيل الدخول |
| رفع الملفات | فحص نوع/حجم وتشفير موجود | إضافة magic-byte validation، أسماء آمنة، حدود فيديو، وفحص محتوى/فيروسات عند توفر خدمة |
| CSRF والجلسات | لا توجد جلسات Cookie حالياً | عند إضافة الجلسات: HttpOnly/Secure/SameSite وCSRF token أو نمط OAuth موثوق |
| الأسرار | محفوظة في Railway ومتجاهلة من Git | تدوير مفتاح OTPIQ لأنه ظهر سابقاً في المحادثة، وتدوير رمز المراجعة |
| الحزم | `pnpm audit --prod --audit-level=high` لم يجد ثغرات معروفة | الاستمرار بفحص آلي دوري وتثبيت الإصدارات |

## نتائج الوظائف

| المجال | الحالة | المطلوب |
|---|---|---|
| إشعارات المواطن | لا يوجد مركز إشعارات دائم بحالات قراءة | جدول إشعارات، unread count، صفحة/درج إشعارات، وإنشاء إشعار مع كل حدث معاملة |
| محرك الخدمات | أربع خدمات فقط وبعض المسارات عامة | تعريف schema موحد للخدمة واستمارة مستقلة لكل خدمة |
| الجواز/البطاقة/السياقة | غير موجودة | إضافتها كطلبات مواعيد/إحالات دون ادعاء إصدار الوثيقة من المنصة قبل تكامل الجهة المختصة |
| حجز موعد | غير موجود | جدول فترات، منع التعارض، تأكيد/إلغاء، وتذكير |
| فيديو الوجه | التقاط فيديو حقيقي موجود دون فرض 7 ثوانٍ أو تحليل | عداد 7 ثوانٍ، تحقق جودة/حركة مساعد، ونتيجة ثقة لا تعتمد وحدها |
| مطابقة الوجه | غير موجودة | تتطلب مزود KYC/Liveness معتمد؛ يمكن إضافة واجهة provider-adapter ونتيجة `REVIEW_REQUIRED` حالياً |
| GIS | 11 جهة بمصادر دون إحداثيات | إدخال الإحداثيات فقط من مصدر رسمي/ملف GIS؛ لا تُنشأ مواقع تخمينية |

## قرارات التنفيذ

سيتم إزالة الشريط القانوني من رأس الصفحات ونقل تنبيه مختصر إلى شروط الاستخدام والتذييل، مع الحفاظ على عدم انتحال الصفة الرسمية. ستبقى مطابقة الوجه **مساعدة** وليست قرار اعتماد نهائي حتى يصل مزود KYC معتمد. خدمات الجواز والبطاقة الموحدة وإجازة السياقة ستُبنى كاستمارات وحجز موعد وإحالة إلى الجهة المختصة، لا كإصدار مزيف لوثائق سيادية.

## ضوابط نُفذت في مرحلة التقوية

تم تقييد CORS إلى نطاق الإنتاج وعناوين التطوير المحلية، وإضافة Helmet مع CSP وHSTS ورفض التضمين داخل الإطارات وسياسة إحالة مقيدة. أُضيفت حدود طلبات عامة وحساسة، وتعطيل التخزين المؤقت لواجهات الهوية والإدارة والمعاملات، ومعرف طلب لكل استجابة وخطأ.

أُنشئت جلسات موقعة بخوارزمية HMAC داخل Cookie من نوع `HttpOnly` و`SameSite=Lax` و`Secure` في الإنتاج. جلسة المواطن تُنشأ بعد نجاح OTP، وجلسة الموظف بعد رمز الوصول، وأُغلقت مسارات ملف المواطن وقائمة المعاملات وقرارات الموظف والوسائط المشفرة حسب الدور.

أضيف فحص لتوقيع محتوى JPEG وPNG وWebP وPDF وMP4/WebM قبل التشفير، فلا يكفي اسم الملف أو `Content-Type` المرسل من المتصفح. كذلك أُزيل مسار إعادة بيانات العرض من الإنتاج، وأصبح الخطأ العام يعيد مرجع متابعة دون كشف تفاصيل الخادم.

> يبقى التحول من رمز موظف مشترك إلى إدارة هوية مؤسسية كاملة (SSO/MFA وحساب فردي لكل موظف) متطلباً قبل الاعتماد الحكومي النهائي. كما يجب تدوير مفتاح OTPIQ ورمز المراجعة بعد اكتمال الترقية لأنهما استُخدما خلال التهيئة الأولية.
