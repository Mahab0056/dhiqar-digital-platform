# تحقق إصلاح تصوير الهوية والوجه

**التاريخ:** 2026-08-27  
**النطاق:** واجهة التقاط وجه الهوية وظهرها وفيديو الوجه 7 ثوانٍ، من دون تغيير مسارات رفع الوسائط المشفرة أو مراجعة الهوية أو الصلاحيات.

## سبب الخلل المرجح

كانت الواجهة تفتح حالة الكاميرا ثم تربط `srcObject` في مهلة زمنية قصيرة من دون انتظار تركيب عنصر الفيديو أو اكتمال تشغيله. وكان زر الالتقاط يستعمل أبعاداً افتراضية عندما لا تكون أبعاد الفيديو جاهزة. على الهواتف، ولا سيما عند تأخر تركيب العنصر أو عند منع التشغيل التلقائي، قد ينتج عن ذلك إطار أسود أو لقطة سوداء.

## التعديل المنفذ

| المجال | التعديل |
|---|---|
| تركيب البث | يجري انتظار عنصر الفيديو عبر `requestAnimationFrame` ثم تعيين `srcObject` وتشغيل `video.play()` بشكل صريح. |
| جاهزية الالتقاط | لا يصبح التقاط الصورة متاحاً قبل وجود أبعاد حقيقية لبث الفيديو. |
| الوجه | لا يبدأ تسجيل الفيديو تلقائياً إلا بعد نجاح تشغيل المعاينة. |
| إعادة المحاولة | إيقاف جميع مسارات البث والتسجيل القديمة قبل فتح محاولة جديدة، ومنع تسجيل قديم من إيقاف بث جديد. |
| رسائل الخطأ | تمييز رفض الإذن، عدم وجود كاميرا، احتلال الكاميرا من تطبيق آخر، غياب HTTPS، وتعذر تشغيل المعاينة. |
| الخصوصية | يستمر إيقاف tracks وإزالة `srcObject` عند الإلغاء أو الانتهاء، ولا تتغير آلية التخزين المشفر أو صلاحيات المراجعة. |

## الفحوصات المنفذة

| الفحص | النتيجة |
|---|---|
| TypeScript وبناء الإنتاج | ناجح. |
| صحة API في بيئة اختبار معزولة | ناجح (`200`). |
| دخول غرفة العمليات بالرمز المؤقت في بيئة اختبار معزولة | ناجح. |
| قراءة مؤشرات غرفة العمليات بالدور التشغيلي | ناجح (`200`). |
| محاولة دور العمليات فتح مراجعات الهوية | مرفوضة من الحارس (`401`)؛ لا يصل الدور إلى بيانات الهوية. |

> لا تمتلك بيئة الاختبار كاميرا هاتف فعلية، لذلك لا يمكن محاكاة البث الضوئي المادي فيها. التعديل يعالج تسلسل تشغيل عنصر الفيديو الذي كان يسبب الإطار الأسود، ويضيف مسارات إيضاح وإعادة محاولة لقياس النتيجة على هاتف المستخدم عبر الرابط المنشور.
