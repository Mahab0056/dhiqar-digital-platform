# استئناف نشر Railway — نقطة العودة

**الحالة:** تم إنجاز التطوير والفحص المحلي. النشر إلى Railway متوقف عمداً إلى حين تسجيل الدخول من حساب المستخدم وإضافة السر المطلوب.

## التغييرات الجاهزة محلياً

توجد ثلاثة commits محلية لم تُدفع بعد إلى المستودع البعيد:

| Commit | المحتوى |
|---|---|
| `4dd1509` | تصميم حديث، سلايدر أخبار، خدمات واستمارات، إشعارات، GIS، تصوير الهوية والفيديو، وتحسينات الحماية. |
| `0936aea` | تقرير فحص محلي. |
| `f783ae3` | استثناء قاعدة البيانات والوسائط ولقطات الجودة من Git. |

## الإجراء المطلوب عند العودة

1. سجّل الدخول إلى Railway وافتح خدمة `dhiqar-digital-platform` داخل مشروع `fabulous-laughter`.
2. أضف متغيراً سرياً باسم `SESSION_SECRET` بقيمة موجودة في الملف المحلي المحمي `/tmp/dhiqar-session-secret.env`.
3. طبّق متغيرات البيئة ثم ادفع commits المحلية إلى `main` في المستودع الخاص.
4. انتظر إصدار Railway حتى يصبح `ACTIVE` ثم افحص `/api/health` والرئيسية وغرفة العمليات وتسجيل دخول الموظف.
5. لا تكشف قيمة السر في Git أو الواجهة أو التوثيق.

## سبب الإيقاف

طبقة الجلسات الآمنة تعتمد `SESSION_SECRET` في بيئة الإنتاج. دفع المصدر قبل وضعه سيؤدي إلى تشغيل أقل أماناً أو فشل بدء الخدمة حسب إعداد البيئة، لذلك تم إيقاف النشر حتى تتم إضافة السر.
