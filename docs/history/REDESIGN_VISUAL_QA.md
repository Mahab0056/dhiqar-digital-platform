# فحص بصري لإعادة التصميم

## سطح المكتب

الهيكل الجديد متوازن: الهيدر واضح، صورة زقورة أور لها حضور مؤسسي، البحث في مركز المهمة، ومؤشرات الإنتاج موضوعة داخل بطاقة واحدة بدل أرقام تسويقية مبالغ بها. شريط الاختصارات واضح وقابل للمسح البصري.

## الهاتف

الترتيب أصبح عمودياً ومنطقياً: الشعار والقائمة، العنوان، البحث، الأزرار، ثم الصورة. لقطة Chromium التقطت عناصر Framer Motion أثناء مرحلة الدخول فظهرت باهتة؛ يجب إعادة اللقطة بميزانية وقت افتراضي لضمان أن البهتان ليس عيباً دائماً. لا يوجد قص أفقي أو تضخم للنص في اللقطة الحالية.

## إعادة الفحص بعد إصلاح الحركة

بعد استبدال `whileInView` بحركة دخول مؤكدة في بطل الصفحة، ظهرت نسخة الهاتف بالتباين الكامل: العنوان أسود/أخضر واضح، البحث والأزرار مقروءة، ولا يوجد قص أفقي أو تداخل. ترتيب المحتوى مناسب للعمل بيد واحدة، والصورة تبدأ بعد عناصر المهمة الأساسية بدلاً من مزاحمتها.

## بوابة المواطن على الهاتف

ظهر شريط التنقل السفلي بأربع مهام واضحة وبحجم لمس مناسب، ولم يحجب بطاقات الصفحة بفضل مساحة السفل المضافة. الهيدر أصبح مختصراً، وعناصر العمل الأساسية تظهر في أول شاشة بدون ازدحام أفقي.

## فحص GIS المحلي

أعاد مسار `/operations` في أحدث نسخة إنتاج محلية حالة HTTP `200` وواجهة الصحة حالة `ok`، لكن لقطة المتصفح الأولى ظهرت بيضاء بلا عناصر أو أخطاء وحدة تحكم ظاهرة. لم يُنشر هذا الإصدار؛ يلزم تشخيص تحميل الواجهة قبل الرفع.

استمر التشخيص: ملف bundle المشار إليه موجود ويُقدّم كـ `text/javascript` بطول صحيح من الخادم المحلي، لكن جذر React بقي فارغاً في المتصفح رغم عدم ظهور خطأ في وحدة التحكم. هذا يعزل المشكلة عن مسار 404 أو نوع المحتوى، ويستلزم إعادة تحميل مع تعطيل الكاش/فحص حالة JavaScript التالية قبل النشر.

المقارنة بعرض Chromium مستقل مع تعطيل الكاش أظهرت أن الجذر ومسار العمليات ومسار تسجيل الدخول جميعها تترك `#root` فارغاً. لذلك لا يخص العطل خريطة GIS أو حاجز الجلسة وحده؛ التشخيص انتقل إلى تنفيذ bundle/سياسة الأمان قبل النشر.

## سلايدر الأخبار في الهوم سكرين

ظهر السلايدر مباشرة بعد البطل وبقبل اختصارات الخدمات، بصورة خبر كبيرة، مصدر ورابط مباشر، عدّاد ونقاط انتقال وأزرار تنقل. في معاينة Vite الحديثة بقي ترتيب الهوم متوازناً على سطح المكتب. تم اختبار التبديل التلقائي بعد 7.6 ثوانٍ وتغيّر الخبر من الفهرس `2` إلى `3` بنجاح.

## ملاحظة معاينة HTTPS المؤقتة

تم فتح proxy HTTPS مؤقت للخادم المحلي، لكن جلسة المتصفح انتقلت إلى صفحة فارغة بعد التحميل ولم توفر لقطة قابلة للفحص. لذلك يعتمد تحقق الواجهة الحالي على معاينة Vite التي عرضت الهوم سكرين وسلايدر الأخبار بنجاح؛ سيُعاد الفحص من رابط Railway HTTPS بعد استعادة جلسة النشر.
