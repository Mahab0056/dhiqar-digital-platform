# اختبار الجودة: الخدمات وغرفة العمليات — 2026-08-27

## النطاق

يغطي هذا الاختبار تحديث مسارات الخدمات المحلية، قرارات الموظف وإشعارات المواطن، فصل دور غرفة العمليات، ومؤشرات الدوائر والحضور والكاميرات. أُجري الاختبار في قاعدة SQLite معزولة مؤقتة؛ لم تُضف بيانات الاختبار إلى قاعدة الإنتاج.

| المسار أو القيد | النتيجة |
|---|---|
| منع إنشاء خدمة الجواز الخارجية من محرك الاستمارات المحلي | ناجح — HTTP `404` |
| إنشاء طلب إجازة بناء محلي | ناجح — تم إنشاء رقم متابعة مستقل |
| ظهور الطلب للموظف | ناجح |
| قرار «طلب نواقص» مع مستند محدد | ناجح |
| وصول حالة النواقص واسم المستند للمواطن | ناجح |
| رفع PDF من المواطن بعد طلب النواقص | ناجح — عاد الطلب إلى `UNDER_REVIEW` |
| اعتماد الموظف للطلب | ناجح |
| وصول إشعار «تمت معاملة الخدمة» للمواطن | ناجح |
| انعكاس المعاملة المعتمدة في مؤشرات غرفة العمليات | ناجح |
| دخول دور `OPERATIONS` | ناجح |
| منع دور العمليات من قائمة الموظفين ومراجعة الهوية | ناجح — HTTP `401` |
| منع دور العمليات من إدخال تعداد الموظفين أو الحضور | ناجح — HTTP `401` |
| إدخال لقطة موظفين من المدير العام | ناجح — HTTP `201` في بيئة الاختبار |
| منع تشغيل كاميرا من دون تفويض وبوابة HTTPS | ناجح — HTTP `400` |
| قراءة حالة الكاميرات من دور العمليات من دون عنوان البوابة | ناجح — HTTP `200`، ولا يعاد `gatewayUrl` |

## ما يظهر في غرفة العمليات

تعرض بطاقة كل دائرة **الطلبات المقدمة، المنجزة، قيد التدقيق، والنواقص** من سجلات المنصة نفسها، مع عدد البلاغات المفتوحة المرتبطة بالدائرة. أما عدد الموظفين والحضور فلا يعرض رقماً ما لم تُسجل لقطة بمرجع مصدر من المدير العام؛ قبلها تعرض البطاقة «بانتظار مصدر حضور مصرح».

زر الكاميرا يعمل كنافذة لحالة التكامل. لا يعرض أي RTSP أو كاميرا تجريبية أو عنوان بوابة. لا يمكن تفعيل كاميرا إلا بعد تسجيل تفويض من الجهة المالكة وبوابة HTTPS من نوع HLS أو WebRTC، وتبقى طبقة البث الخادمية وإدارة الوصول مطلوبة قبل عرض فيديو حي.

## ملاحظات النشر

تم تدوير `ADMIN_REVIEW_PASSWORD` و`OPERATIONS_PASSWORD` في Railway بعد موافقة المستخدم. القيم لم تُقرأ أو تُخزن في المستودع أو وثائق الاختبار. يحتاج أصحاب الحسابات المخولون إلى استلام الرموز عبر قناة إدارية آمنة خارج هذه المحادثة.

اكتمل `pnpm build` بنجاح. يوجد تحذير Vite فقط لأن جزء JavaScript الرئيسي أكبر من `500 KB` بعد الضغط؛ لا يمنع التشغيل لكنه تحسين أداء مقترح في إصدار لاحق.
