# حالة النشر الدائم — Railway

**آخر تحديث:** 26 آب 2026، 23:14 GMT+3

| البند | الحالة |
|---|---|
| المستودع | `Mahab0056/dhiqar-digital-platform` — خاص |
| المزود | Railway — Hobby Plan |
| المشروع | `fabulous-laughter` |
| الخدمة | `dhiqar-digital-platform` |
| المنطقة | EU West |
| النسخ | 1 Replica |
| النشر | قيد الإقلاع الأول |
| رابط Railway المخصص | `https://dhiqar-digital-platform-production.up.railway.app` |

## سجل الإقلاع الذي تم التحقق منه

أظهرت سجلات Railway بدء حاوية Node بنجاح وتشغيل الأمر `tsx server/index.ts`، ثم ظهور الرسالة:

```text
Dhi Qar Digital Demo API listening on http://0.0.0.0:8080
```

كما أظهرت السجلات تركيب Volume للخدمة. ما زال مطلوباً اختبار الرابط العام والتحقق من تعيين `DATABASE_PATH` لمسار الـVolume داخل الخدمة، ثم تنفيذ اختبار كتابة واسترجاع للبيانات للتأكد من الاستمرارية بعد إعادة التشغيل.

## التحقق العام

تم فتح الرابط العام بنجاح من متصفح خارجي:

```text
https://dhiqar-digital-platform-production.up.railway.app
```

أعاد الخادم الصفحة الرئيسية بعنوان **ذي قار الرقمية | THI QAR DIGITAL**، وظهرت واجهة RTL والهوية البصرية والصور وكتالوج الخدمات ومؤشرات التشغيل بصورة سليمة. يؤكد ذلك أن Railway يقدّم الواجهة المبنية وExpress من رابط HTTPS دائم، وليس من بيئة التطوير المحلية.

## فحص API وحساب المواطن

أعادت واجهة الصحة العامة الاستجابة التالية من Railway مع HTTP ناجح:

```json
{"status":"ok","service":"Dhi Qar Digital Demo API"}
```

كما تم فتح `/citizen` من الرابط الدائم، وظهر ملف المواطن التجريبي وملخص المعاملات والوثائق والمدفوعات، وكانت القيم الابتدائية صفراً. يؤكد ذلك أن واجهة React تتصل بـExpress وSQLite المستضافة وليست ببيانات ثابتة داخل المتصفح.

## اختبار كتابة قاعدة البيانات — تجهيز المعاملة

تم فتح نموذج إجازة فتح محل من `/service/store-license` على رابط Railway العام. ظهرت بيانات المواطن الموثقة وقيم GIS والرسوم والمرفقات بصورة صحيحة، وتمت إضافة عقد الإيجار التجريبي من الواجهة. الخطوة التالية هي إرسال المعاملة للتحقق من الكتابة إلى SQLite المستضافة على Volume.

## اختبار كتابة واسترجاع البيانات

تم إرسال معاملة تجريبية من بوابة المواطن على الرابط الدائم. أنشأت الخدمة المرجع `TQD-2026-0001`، وحفظت بيانات المحل والرسم وأحداث التقديم والتوجيه. بعد الانتقال إلى `/employee` ظهرت المعاملة نفسها فوراً في قائمة الموظفة بحالة «تم التقديم»، مع رقم وطني مقنّع وإجراءات التدقيق. يثبت ذلك أن البيانات تُكتب في قاعدة مركزية وتُقرأ من البوابتين على Railway.

## إكمال السيناريو من البداية إلى النهاية

من بوابة الموظف تم اعتماد المعاملة التجريبية بنجاح. تحولت الحالة إلى «مكتملة»، وسُجل الرسم البالغ 75,000 د.ع كمدفوع داخل السيناريو التجريبي، وصدر رقم الوثيقة `LIC-2026-00001` ومعرّف تحقق مستقل `TQD-A44019621E064E9B90`.

فتحت صفحة المواطن النهائية من الرابط الدائم، وظهرت رحلة المعاملة الكاملة، وقالب الوثيقة العربية، وشارة DEMO، وزر PDF، ورمز QR. هذه البيانات صناعية وواضحة بأنها غير صالحة للاستعمال الرسمي.

## التحقق العام من الوثيقة

تم فتح صفحة التحقق العامة عبر معرّف QR على الرابط الدائم. عرضت الصفحة نوع الوثيقة ورقمها ورقم المعاملة واسم صاحبها وحالتها وتاريخها ومعرّف التحقق، مع توضيح بارز أن الوثيقة تخص بيئة العرض وغير صالحة للاستخدام الرسمي. لم تعرض الصفحة الرقم الوطني أو صور الهوية أو أي تفاصيل دفع.

## إعادة النشر بعد التوثيق

أظهر Railway أن إصدار **Persist SQLite on Railway volume** أصبح `ACTIVE` ونجح نشره. ثم بدأ إصدار جديد تلقائياً من Commit التوثيق الأخير. سيتم بعد اكتماله إعادة فتح معاملة `TQD-2026-0001` للتحقق من بقاء السجل عبر دورة نشر فعلية.

## اختبار الموبايل على الإنتاج

تم التقاط صفحة الإنتاج بقياس 390×844. ظهرت الشارة التجريبية وشريط الهوية وزر القائمة بصورة متجاوبة، لكن أداة التصوير الرأسية التقطت منطقة البطل قبل استقرار النص الرئيسي كاملاً. لأن صفحة المتصفح الحية أظهرت المحتوى والروابط بوضوح، سيُعاد فحص لقطة مؤجلة/كاملة قبل اعتبار ذلك مشكلة تصميم أو إجراء أي تغيير على CSS.

## تشخيص دورة Railway

سجل نشر `Persist SQLite on Railway volume` أظهر تركيب الـVolume ثم تشغيل الحاوية بنجاح على المنفذ `8080`، لكن Railway أوقف الحاوية عند بدء نشر لاحق وفق التسلسل التالي:

```text
Mounting volume …
Starting Container
$ tsx server/index.ts
Dhi Qar Digital Demo API listening on http://0.0.0.0:8080
Stopping Container
[LIFECYCLE] Command failed.
```

كانت خدمة التوثيق الأحدث ما تزال في مرحلة النشر عند وقت الفحص. يلزم التمييز بين إيقاف الإصدار السابق نتيجة تبديله بالإصدار الجديد وبين فشل الإقلاع الفعلي للإصدار الأحدث، وذلك بعد اكتمال سجل النشر الحالي.

## حالة الاستضافة النهائية

أصبح الإصدار **Document Railway deployment verification** في Railway بحالة `ACTIVE`، والخدمة ظاهرة `Online` في منطقة EU West بنسخة تشغيل واحدة وVolume متصل. الإصدارات السابقة تظهر كـ`REMOVED` لأن Railway استبدلها بالإصدار النشط خلال النشر المتتابع، لا كخطأ باقٍ في الخدمة.

## اختبار الاستمرارية بعد إعادة النشر

بعد أن أصبح الإصدار الأخير `ACTIVE`، تم فتح المعاملة `TQD-2026-0001` مرة أخرى من الرابط العام وظهرت مكتملة مع الوثيقة `LIC-2026-00001` ومعرّف التحقق نفسه. كما أعادت `/api/health` الحالة `ok` في 26 آب 2026، 20:26 UTC. يثبت ذلك أن SQLite المكتوبة على Railway Volume احتفظت بالبيانات عبر استبدال إصدار الخدمة.


## فحص بعد تحديث الأخبار والإدارة — 27 آب 2026

تم فتح الرابط العام بنجاح وعرض الصفحة الرئيسية من Railway. وقت الفحص، ظهرت واجهة الإصدار السابق حتى قسم «صورة تشغيلية واحدة للمحافظة» ولم تظهر أقسام الأخبار والمناقصات الجديدة؛ لذا يلزم انتظار اكتمال أو تفعيل دورة النشر التلقائية لآخر commit ثم إعادة الفحص قبل اعتبار هذه الأقسام منشورة.

## جلسة Railway لإضافة مفتاح الجلسة

بعد موافقة المستخدم على إضافة `SESSION_SECRET`، أعاد رابط إعدادات الخدمة صفحة `404 / Login` في جلسة المتصفح الحالية. لم يُدخل المفتاح ولم يُنشر أي تغيير في هذه المحاولة. يلزم تسجيل الدخول إلى Railway ثم فتح لوحة المشروع مرة أخرى لإتمام التفعيل والنشر.
